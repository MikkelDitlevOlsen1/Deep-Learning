#!/bin/sh
#BSUB -J torch
#BSUB -o torch_%J.out
#BSUB -e torch_%J.err
#BSUB -q hpc
#BSUB -n 1
#BSUB -R "rusage[mem=12G]"
#BSUB -R "span[hosts=1]"
#BSUB -W 100
# end of BSUB options

# load a scipy module
# replace VERSION and uncomment
# module load scipy/VERSION

# activate the virtual environment 
# NOTE: needs to have been built with the same SciPy version above!
cd Desktop/Fagpro/


source fag1-env/bin/activate
python Github/test1/lstm_hpc.py
